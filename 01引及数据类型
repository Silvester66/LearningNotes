\#预处理
stdio
std 标准
io输入输出
<> 系统提供的
"" 当前工程下找，没找到去系统找

<< 插入流，也是函数

标识符命名规则，字母数字下划线，不能数字开头

存在哪里——内存模型
运行时分成四个区——
代码区，数据区，堆，栈（局部变量存于此）
（进程）

数据类型
不同类型，内存；操作不同。值集，操作。简单类型；复合类型（指针，引用。+修饰符）；自定义（结构体/）

阶码?
数据精度，浮点数会有误差

char
不能直接存a，而是转化成数字（ASC码）。1个字节，最高位为符号位
UNSI，无符号位的。
